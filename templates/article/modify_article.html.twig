{% extends 'base.html.twig' %}
{% block body %}
{% form_theme form 'form/form/bootstrap_5_layout.html.twig' %}
<h1> Modifier l'article</h1>
    {{ form_start(form) }}

    <div>
    {{form_row(form.title)}}
    </div>

    <div>
    {{form_row(form.content)}}
    </div>

    <div class="form-group">
    {{form_row(form.category)}}
    </div>

    {{ form_end(form) }}
{% endblock %}